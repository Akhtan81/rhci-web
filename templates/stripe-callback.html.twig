{% extends 'base.html.twig' %}

{% block body %}
    <div class="banner">
        <h3>{{ 'callbacks.stripe.title'|trans }}</h3>
        <h4>{{ 'callbacks.stripe.footer'|trans }}</h4>

        <a href="{{ path('index') }}" class="btn btn-success">{{ 'navigation.homepage'|trans }}</a>
    </div>
{% endblock %}