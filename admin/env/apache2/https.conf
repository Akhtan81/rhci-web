<VirtualHost *:443>

    ServerName admin.tabee.click
    DocumentRoot /var/www/html/public

    ErrorLog ${APACHE_LOG_DIR}/https-error.log
    CustomLog ${APACHE_LOG_DIR}/https-access.log combined

    SetEnvIf Authorization "(.*)" HTTP_AUTHORIZATION=$1

    SSLEngine on

    SSLCertificateFile /etc/letsencrypt/live/admin.tabee.click/cert.pem
    SSLCertificateKeyFile /etc/letsencrypt/live/admin.tabee.click/privkey.pem
    SSLCertificateChainFile /etc/letsencrypt/live/admin.tabee.click/fullchain.pem

    <Directory /var/www/html/public>
        AllowOverride None
        Order Allow,Deny
        Allow from All

        Options -MultiViews
        RewriteEngine On
        RewriteCond %{REQUEST_FILENAME} !-f
        RewriteRule ^(.*)$ index.php [QSA,L]

    </Directory>

    <Directory ~/.well-known/acme-challenge>
        Allow from All
    </Directory>

</VirtualHost>
